(function () {
    'use strict';

    var gulp = require('gulp'),
        serve = require('gulp-serve');

    gulp.task('serve', serve({
        root: '<%= sourceDir %>',
        port: <%= serverPort %>
    }));

    gulp.task('default', ['serve']);

}());
